
function greeter(person){
    return "Hello," + person;
}
let user = "June User";
document.body.textContent = greeter(user);


function greeter(person: string){
    return "Hello," + person;
}
let user = [0, 1 2];
document.body.textContent = greeter(user);